#include "oltreap.h"

void olVerifyTree(olNode node);
void olVerifyOrderedList(olRoot root);
void olDumpTree(olNode node, uint32 depth);
void olDumpOrderedList(olRoot root);
void olReportStats(olRoot root);
